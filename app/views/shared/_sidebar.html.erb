
    <div class="col-xs-3">

    <h2>User info</h2>
     <%= attachment_image_tag(user, :profile_image, :fill, 60, 60, format: 'jpeg', class: "img-circle pull-left profile-img", fallback: "no_image.jpg") %>
     <table class="table">
      <tbody><tr></tr>
        <tr>
          <th>name</th>

          <th><%= user.name %></th>
        </tr>
        <tr>
          <th>introduction</th>
          <th><%= user.introduction %></th>
        </tr>
      </tbody></table>

    <%= link_to "", edit_user_path(user.id), class: "col-xs-12 btn btn-default glyphicon glyphicon-wrench" %></a>

 <h2>New book</h2>

<%= form_for(book, url: books_path) do |f| %>
   <% if book.errors.any? %>
   <h2><%= book.errors.count %>件のエラーが発生しました。</h2>
   <% book.errors.full_messages.each do |message| %>
   <li><%= message %></li>
   <% end %>
   <% end %>
   <label>Title</label>
   <%= f.text_field :title, class:"form-control", placeholder:"title" %>
   <label>opinion</label>
   <%= f.text_field :body, class:"form-control", rows:"3", placeholder:"opinion" %>
   <br>
  <%= f.submit "Create Book", class:"btn btn-primary col-xs-12" %>
  <% end %>
</div>